rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permissive rules for sandbox/testing environments to ensure no permission blocks
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
